import { CourseStatusDirective } from './course-status.directive';

describe('CourseStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseStatusDirective();
    expect(directive).toBeTruthy();
  });
});
